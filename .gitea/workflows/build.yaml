name: Build OpenMemory images

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - packages/openmemory-js/**
      - dashboard/**
      - .gitea/workflows/build.yaml

env:
  REGISTRY: git.itsa.house
  REGISTRY_USER: rbrenner
  API_IMAGE: git.itsa.house/homelab/openmemory-api
  DASHBOARD_IMAGE: git.itsa.house/homelab/openmemory-dashboard
  K8S_NAMESPACE: openmemory
  API_DEPLOYMENT: openmemory-api
  DASHBOARD_DEPLOYMENT: openmemory-dashboard

jobs:
  build-api:
    runs-on: self-hosted
    steps:
      - name: Setup
        run: |
          buildah --version
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          echo "SHORT_SHA=${SHORT_SHA}" >> "$GITHUB_ENV"

      - name: Checkout
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
        run: |
          REPO_URL="${{ github.server_url }}/${{ github.repository }}.git"
          AUTH_URL=$(echo "$REPO_URL" | sed "s|https://|https://token:${DEPLOY_TOKEN}@|")
          rm -rf checkout && git clone --depth=1 "$AUTH_URL" checkout

      - name: Login to registry
        env:
          REGISTRY_TOKEN: ${{ secrets.REGISTRY_TOKEN }}
        run: |
          buildah login -u "${{ env.REGISTRY_USER }}" -p "${REGISTRY_TOKEN}" "${{ env.REGISTRY }}"

      - name: Build API image
        run: |
          cd checkout/packages/openmemory-js
          buildah bud --isolation=chroot \
            -t "${{ env.API_IMAGE }}:latest" \
            -t "${{ env.API_IMAGE }}:${SHORT_SHA}" \
            -f Dockerfile .

      - name: Push API image
        run: |
          push_retry() {
            local img=$1 attempt=0
            while [ $attempt -lt 3 ]; do
              attempt=$((attempt + 1))
              echo "Push attempt $attempt/3: $img"
              if buildah push "$img"; then return 0; fi
              echo "Attempt $attempt failed, retrying in 15s..."
              sleep 15
            done
            echo "Push failed after 3 attempts: $img"
            return 1
          }
          push_retry "${{ env.API_IMAGE }}:latest"
          push_retry "${{ env.API_IMAGE }}:${SHORT_SHA}"

      - name: Validate API image
        run: |
          buildah pull "${{ env.API_IMAGE }}:${SHORT_SHA}"
          buildah inspect "${{ env.API_IMAGE }}:${SHORT_SHA}" | jq -r '.OCIv1.config.Entrypoint'
          echo "VALIDATED: ${{ env.API_DEPLOYMENT }}:${SHORT_SHA}"

      - name: Deploy API to k3s
        run: |
          # Use in-cluster ServiceAccount token to patch the deployment via the k8s API.
          # This triggers a rollout restart without needing kubectl or the k3s socket.
          K8S_API="https://${KUBERNETES_SERVICE_HOST}:${KUBERNETES_SERVICE_PORT}"
          TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
          CACERT="/var/run/secrets/kubernetes.io/serviceaccount/ca.crt"
          NAMESPACE="${{ env.K8S_NAMESPACE }}"
          DEPLOYMENT="${{ env.API_DEPLOYMENT }}"
          RESTART_TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          PATCH="{\"spec\":{\"template\":{\"metadata\":{\"annotations\":{\"kubectl.kubernetes.io/restartedAt\":\"${RESTART_TS}\"}}}}}"
          echo "Patching ${NAMESPACE}/${DEPLOYMENT} to trigger rollout..."
          HTTP_STATUS=$(curl -sf -o /tmp/patch_response.json -w "%{http_code}" \
            -X PATCH \
            -H "Authorization: Bearer ${TOKEN}" \
            -H "Content-Type: application/strategic-merge-patch+json" \
            --cacert "${CACERT}" \
            "${K8S_API}/apis/apps/v1/namespaces/${NAMESPACE}/deployments/${DEPLOYMENT}" \
            -d "${PATCH}")
          echo "API response status: ${HTTP_STATUS}"
          if [ "${HTTP_STATUS}" != "200" ]; then
            cat /tmp/patch_response.json
            exit 1
          fi
          # Poll rollout status via the API until all replicas are ready (timeout 240s)
          DEADLINE=$(($(date +%s) + 240))
          while [ "$(date +%s)" -lt "$DEADLINE" ]; do
            DEPLOY_JSON=$(curl -sf \
              -H "Authorization: Bearer ${TOKEN}" \
              --cacert "${CACERT}" \
              "${K8S_API}/apis/apps/v1/namespaces/${NAMESPACE}/deployments/${DEPLOYMENT}")
            READY=$(echo "${DEPLOY_JSON}" | jq -r '.status.readyReplicas // 0')
            DESIRED=$(echo "${DEPLOY_JSON}" | jq -r '.spec.replicas // 1')
            echo "Rollout status: ${READY:-0}/${DESIRED:-?} replicas ready"
            if [ "${READY}" = "${DESIRED}" ] && [ -n "${READY}" ]; then
              echo "DEPLOYED: openmemory-api:${SHORT_SHA}"
              exit 0
            fi
            sleep 5
          done
          echo "ERROR: Rollout did not complete within 240s"
          exit 1

      - name: Notify success
        if: success()
        env:
          WEBHOOK_URL: ${{ secrets.MATTERMOST_AGENT_WEBHOOK }}
        run: |
          curl -sf -X POST "$WEBHOOK_URL" \
            -H 'Content-Type: application/json' \
            -d "{\"text\": \"**[ci]** Built ${{ env.API_DEPLOYMENT }}:${SHORT_SHA} (buildah)\", \"username\": \"Gitea CI\"}" || true

      - name: Notify failure
        if: failure()
        env:
          WEBHOOK_URL: ${{ secrets.MATTERMOST_ALERTS_WEBHOOK }}
        run: |
          curl -sf -X POST "$WEBHOOK_URL" \
            -H 'Content-Type: application/json' \
            -d "{\"text\": \"**[ci] FAILED** :rotating_light: ${{ env.API_DEPLOYMENT }} build\nRun: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\", \"username\": \"Gitea CI\"}" || true

  build-dashboard:
    runs-on: self-hosted
    steps:
      - name: Setup
        run: |
          buildah --version
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          echo "SHORT_SHA=${SHORT_SHA}" >> "$GITHUB_ENV"

      - name: Checkout
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
        run: |
          REPO_URL="${{ github.server_url }}/${{ github.repository }}.git"
          AUTH_URL=$(echo "$REPO_URL" | sed "s|https://|https://token:${DEPLOY_TOKEN}@|")
          rm -rf checkout && git clone --depth=1 "$AUTH_URL" checkout

      - name: Login to registry
        env:
          REGISTRY_TOKEN: ${{ secrets.REGISTRY_TOKEN }}
        run: |
          buildah login -u "${{ env.REGISTRY_USER }}" -p "${REGISTRY_TOKEN}" "${{ env.REGISTRY }}"

      - name: Build dashboard image
        run: |
          cd checkout/dashboard
          buildah bud --isolation=chroot \
            -t "${{ env.DASHBOARD_IMAGE }}:latest" \
            -t "${{ env.DASHBOARD_IMAGE }}:${SHORT_SHA}" \
            -f Dockerfile .

      - name: Push dashboard image
        run: |
          push_retry() {
            local img=$1 attempt=0
            while [ $attempt -lt 3 ]; do
              attempt=$((attempt + 1))
              echo "Push attempt $attempt/3: $img"
              if buildah push "$img"; then return 0; fi
              echo "Attempt $attempt failed, retrying in 15s..."
              sleep 15
            done
            echo "Push failed after 3 attempts: $img"
            return 1
          }
          push_retry "${{ env.DASHBOARD_IMAGE }}:latest"
          push_retry "${{ env.DASHBOARD_IMAGE }}:${SHORT_SHA}"

      - name: Validate dashboard image
        run: |
          buildah pull "${{ env.DASHBOARD_IMAGE }}:${SHORT_SHA}"
          buildah inspect "${{ env.DASHBOARD_IMAGE }}:${SHORT_SHA}" | jq -r '.OCIv1.config.Cmd'
          echo "VALIDATED: ${{ env.DASHBOARD_DEPLOYMENT }}:${SHORT_SHA}"

      - name: Deploy dashboard to k3s
        run: |
          K8S_API="https://${KUBERNETES_SERVICE_HOST}:${KUBERNETES_SERVICE_PORT}"
          TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
          CACERT="/var/run/secrets/kubernetes.io/serviceaccount/ca.crt"
          NAMESPACE="${{ env.K8S_NAMESPACE }}"
          DEPLOYMENT="${{ env.DASHBOARD_DEPLOYMENT }}"
          RESTART_TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          PATCH="{\"spec\":{\"template\":{\"metadata\":{\"annotations\":{\"kubectl.kubernetes.io/restartedAt\":\"${RESTART_TS}\"}}}}}"
          echo "Patching ${NAMESPACE}/${DEPLOYMENT} to trigger rollout..."
          HTTP_STATUS=$(curl -sf -o /tmp/patch_response.json -w "%{http_code}" \
            -X PATCH \
            -H "Authorization: Bearer ${TOKEN}" \
            -H "Content-Type: application/strategic-merge-patch+json" \
            --cacert "${CACERT}" \
            "${K8S_API}/apis/apps/v1/namespaces/${NAMESPACE}/deployments/${DEPLOYMENT}" \
            -d "${PATCH}")
          echo "API response status: ${HTTP_STATUS}"
          if [ "${HTTP_STATUS}" != "200" ]; then
            cat /tmp/patch_response.json
            exit 1
          fi
          DEADLINE=$(($(date +%s) + 240))
          while [ "$(date +%s)" -lt "$DEADLINE" ]; do
            DEPLOY_JSON=$(curl -sf \
              -H "Authorization: Bearer ${TOKEN}" \
              --cacert "${CACERT}" \
              "${K8S_API}/apis/apps/v1/namespaces/${NAMESPACE}/deployments/${DEPLOYMENT}")
            READY=$(echo "${DEPLOY_JSON}" | jq -r '.status.readyReplicas // 0')
            DESIRED=$(echo "${DEPLOY_JSON}" | jq -r '.spec.replicas // 1')
            echo "Rollout status: ${READY:-0}/${DESIRED:-?} replicas ready"
            if [ "${READY}" = "${DESIRED}" ] && [ -n "${READY}" ]; then
              echo "DEPLOYED: openmemory-dashboard:${SHORT_SHA}"
              exit 0
            fi
            sleep 5
          done
          echo "ERROR: Rollout did not complete within 240s"
          exit 1

      - name: Notify success
        if: success()
        env:
          WEBHOOK_URL: ${{ secrets.MATTERMOST_AGENT_WEBHOOK }}
        run: |
          curl -sf -X POST "$WEBHOOK_URL" \
            -H 'Content-Type: application/json' \
            -d "{\"text\": \"**[ci]** Built ${{ env.DASHBOARD_DEPLOYMENT }}:${SHORT_SHA} (buildah)\", \"username\": \"Gitea CI\"}" || true

      - name: Notify failure
        if: failure()
        env:
          WEBHOOK_URL: ${{ secrets.MATTERMOST_ALERTS_WEBHOOK }}
        run: |
          curl -sf -X POST "$WEBHOOK_URL" \
            -H 'Content-Type: application/json' \
            -d "{\"text\": \"**[ci] FAILED** :rotating_light: ${{ env.DASHBOARD_DEPLOYMENT }} build\nRun: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\", \"username\": \"Gitea CI\"}" || true
